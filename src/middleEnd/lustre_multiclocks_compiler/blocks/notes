(*Please add block in Alphabetic order*)
Ktrinh: Assignment,
        Bias (pp),
        Concatenate,
        Demux,
        DotProduct(pp),
        Gain (pp), 
        Goto,
        Logic,
        Math,
        Mux,
        Product,
        Reshape,
        Switch,
        Sqrt, 
        Trigonomery, 
        
        
        



Hamza:  Abs,
        Constant,
        Clock (pp),
        CompareTo (Masked Subsystem), 
        CompareToConstant (Masked Subsystem),
        Dead Zone (pp)
        Dead Zone Dynamic (pp),
        Delay,
        DigitalClock (pp),
        DiscreteIntegrator (pp),
        DiscreteStateSpace (pp),
        Display (ignored)
        EnablePort,
        Fcn (pp) (*TODO: need to be translated when pp fails*)
        ForEach (pp),
        From,
        Integrator (pp),
        Lookup (pp),
        Lookup_n-D (pp for n ==1 )(*TODO: for n > 1*)
        MinMax,
        Outport,
        Quantizer (pp),
        RateTransition (pp),
        RelationalOperator, 
        Relay (pp),
        ResetPort, 
        Sum,
        Subsystem,
        Saturation (pp), 
        Saturation dynamic (pp), 
        Selector (pp),
        Sigbuilder block (pp),
        Terminator (ignored),
        ToWorkspace (pp),
        TransferFcn (pp),
        TriggerPort, 
        UnitDelay,
        ZeroPole (pp),
        

Blocks that has left from Biosentinel:
    ActionPort
    Assignment
    BusAssignment
    BusCreator
    BusSelector
    DataTypeConversion
    DataTypeDuplicate
    Demux
    DiscreteTransferFcn (see TransferFcn in pp)
    Fcn (partially done in pp)
    ForIterator
    FromWorkspace
    If
    Lookup\_n-D
    Memory (can be descritized in pp to unitDelay) 
    Merge
    ModelReference
    MultiPortSwitch
    Mux
    RandomNumber ( to translate to empty node with contract)
    Rounding
    S-Function
    SampleTimeMath
    Selector (see pp)
    StateControl
    SwitchCase
    