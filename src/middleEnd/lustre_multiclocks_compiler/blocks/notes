(*Please add block in Alphabetic order*)
Ktrinh: Assignment,
        Bias (pp),
        BusAssignment
        BusCreator
        BusSelector
        Concatenate,
        DataTypeConversion 
        DataTypeDuplicate (ignored),
        Demux,
        DotProduct(pp),
        FromWorkspace,
        Gain (pp), 
        Goto,
        Logic,
        Lookup\_n-D (Khanh)
        Math,
        MultiPortSwitch
        Mux,
        Product,
        Reshape,
        Rounding,
        Selector (see pp) (Khanh)
        Switch,
        Sqrt, 
        Trigonomery, 
        
        
        



Hamza:  Abs,
        ActionPort,
        Constant,
        Clock,
        CompareTo (Masked Subsystem), 
        CompareToConstant (Masked Subsystem),
        Dead Zone (pp)
        Dead Zone Dynamic (pp),
        Delay,
        DigitalClock,
        DiscreteIntegrator (pp),
        DiscreteStateSpace (pp),
        Display (ignored)
        EnablePort,
        Fcn (pp) (*TODO: need to be translated when pp fails*)
        ForEach (pp),
        From,
        If,
        Integrator (pp),
        Memory (pp)
        MinMax,
        Outport,
        Quantizer (pp),
        RelationalOperator, 
        Relay (pp),
        ResetPort, 
        Scope(ignored)
        Sum,
        Subsystem,
        Saturation (pp), 
        Saturation dynamic (pp), 
        Selector (pp),
        Sigbuilder block (pp),
        SwitchCase
        Terminator (ignored),
        ToWorkspace (pp),
        TransferFcn (pp),
        TriggerPort, 
        UnitDelay,
        ZeroPole (pp),
        

Blocks that has left from Biosentinel:
   
    BusCreator, 
    BusSelector
    DiscreteTransferFcn (see TransferFcn in pp) (Khanh)
    
    Fcn (partially done in pp) (Khanh)
    ForIterator (Hamza)
    Merge (Hamza)
    ModelReference (Hamza)
    RandomNumber ( to translate to empty node with contract) (Hamza)
    RateTransition (check the pp, Khanh),
    S-Function (Hamza)
    SampleTimeMath (Hamza)
    StateControl 
    
AVA mission Simulink blocks:
    CombinatorialLogic
    DiscreteFilter
    DiscreteFir
    DiscretePulseGenerator
    SignalConversion
    Signum
    Step
    WhileIterator

LM Challenger new blocks 
    {'InportShadow'      }
    {'SignalConversion'  }
   