(*Please add block in Alphabetic order*)
Ktrinh: Assignment,
        Bias (pp),
        Concatenate,
        DataTypeConversion 
        DataTypeDuplicate (ignored),
        Demux,
        DotProduct(pp),
        FromWorkspace,
        Gain (pp), 
        Goto,
        Logic,
        Lookup\_n-D (Khanh)
        LookupTableDynamic,
        Math,
        MultiPortSwitch
        Mux,
        Product,
        Reshape,
        Rounding,
        Selector (see pp) (Khanh)
        Switch,
        Sqrt, 
        Trigonomery, 
        
        
        



Hamza:  Abs,
        ActionPort,
        BusCreator, 
        BusSelector, 
        Constant,
        Clock,
        CombinatorialLogic,
        CompareTo (Masked Subsystem), 
        CompareToConstant (Masked Subsystem),
        Dead Zone (pp)
        Dead Zone Dynamic (pp),
        Delay,
        DigitalClock,
        DiscreteIntegrator (pp),
        DiscreteStateSpace (pp),
        Display (ignored)
        EnablePort,
        Fcn (pp) (*TODO: need to be translated when pp fails*)
        ForEach (pp),
        From,
        If,
        InportShadow(pp),
        Integrator (pp),
        Memory (pp)
        Merge,
        MinMax,
        Outport,
        Quantizer (pp),
        RateTransition,
        RelationalOperator, 
        Relay (pp),
        ResetPort, 
        SampleTimeMath (pp)
        Scope(ignored)
        Sum,
        Subsystem,
        Saturation (pp), 
        Saturation dynamic (pp), 
        Selector (pp),
        Sigbuilder block (pp),
        SignalConversion,
        Signum,
        SwitchCase
        Terminator (ignored),
        ToWorkspace (pp),
        TransferFcn (pp),
        TriggerPort, 
        UnitDelay,
        ZeroPole (pp),
        

Blocks that has left from Biosentinel:

    ForIterator (Hamza)

    Tapped Delay Line
    Bitwise Operator
    UnaryMinus
    
    BusAssignment (Hamza)
    **************************** PP ***********************
    Using the same algorithm A, B, C, D
    DiscreteTransferFcn (see TransferFcn in pp) (Khanh)
    DiscreteStateSpace (Khanh).
    ZeroPole(Khanh)
    DiscreteFilter(Khanh)
    DiscreteFir

    **************************** PP ***********************
    Pulse generator, DiscretePulseGenerator, SignalBuilder pp (they use datatable_process which can be replaced by Lustre code) 

    Fcn (partially done in pp) (Khanh)
   
    
    ModelReference (Hamza)
    RandomNumber ( to translate to empty node with contract) (Hamza)
    

    S-Function (Hamza)
    StateControl 
    
AVA mission Simulink blocks:
    Step (Hamza)
    WhileIterator

   